{
  "name": "purple-rain",
  "version": "1.0.0",
  "type": "module",
  "main": "build/index.js",
  "private": true,
  "scripts": {
    "clean": "rimraf build",
    "build": "rimraf build && swc src --strip-leading-paths -s -d build && tsc-alias -f"
  },
  "devDependencies": {
    "@swc/cli": "^0.3.12",
    "@swc/core": "^1.4.13",
    "rimraf": "^5.0.5",
    "tsc-alias": "^1.8.8"
  }
}