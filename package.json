{
  "name": "purple-rain",
  "version": "1.0.0",
  "type": "module",
  "main": "build/index.js",
  "private": true,
  "scripts": {
    "clean": "rimraf build",
    "build": "rimraf build && swc src --strip-leading-paths -s -d build && tsc-alias -f"
  },
  "devDependencies": {
    "@swc/cli": "^0.3.12",
    "@swc/core": "^1.4.13",
    "pino-pretty": "^11.0.0",
    "rimraf": "^5.0.5",
    "tsc-alias": "^1.8.8"
  },
  "dependencies": {
    "discord.js": "^14.14.1",
    "pino": "^8.20.0",
    "yaml": "^2.4.1"
  }
}